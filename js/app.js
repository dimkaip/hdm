document.addEventListener('DOMContentLoaded', function () {
  console.log('CivicGraph app.js loaded');

  if (typeof graphData !== 'undefined') {
    console.log('Graph data available:', graphData);
  }
});
